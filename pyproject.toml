[project]
name = "claude-code-config"
version = "0.1.0"
description = "Claude Code configuration files"
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
