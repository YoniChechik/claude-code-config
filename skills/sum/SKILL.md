---
name: "sum"
description: "Summarize current branch work grouped by actual tasks (not by commits)"
argument-hint: "[optional: base branch, defaults to main]"
---

# Branch Task Summary

Summarize what was accomplished on the current branch, grouped by **logical tasks/features**, not by individual commits.

## Optional argument
"$ARGUMENTS"

If provided, treat it as the base branch to diff against. Otherwise default to `main`.
